import { DisplayInstructionsPipe } from './display-instructions.pipe';

describe('DisplayInstructionsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayInstructionsPipe();
    expect(pipe).toBeTruthy();
  });
});
